# Used by docker-compose for local dev only.
# Production config is set in the Home Assistant add-on UI.

PUBLIC_BASE_URL=http://localhost:3400
PORT=3400

# Host path for downloaded MP3 files (bind-mounted into the container)
AUDIO_PATH=./audio
